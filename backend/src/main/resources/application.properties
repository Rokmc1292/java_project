# ========================================
# ?? ?? ?? ? URL
# ========================================

# ?? ??
server.port=${SERVER_PORT:8080}

# ???? ??
server.servlet.context-path=/

# ?????? ?? URL
app.base-url=${APP_BASE_URL:http://localhost:8080}

# ?? ?? ??
server.servlet.session.timeout=86400s
server.servlet.session.cookie.name=SESSIONID
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=none

# ========================================
# 스케줄링 설정
# ========================================

# 크롤링 스케줄러 활성화 (유료 플랜)
app.scheduling.enabled=${ENABLE_SCHEDULING:true}

# ========================================
# ?????? ??
# ========================================

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================================
# HikariCP Connection Pool 설정
# (DB 연결 끊김 방지)
# ========================================

# 연결 테스트 쿼리 - 연결이 유효한지 확인
spring.datasource.hikari.connection-test-query=SELECT 1

# 최대 연결 수
spring.datasource.hikari.maximum-pool-size=10

# 최소 유휴 연결 수
spring.datasource.hikari.minimum-idle=5

# 연결 최대 수명 (30분) - MySQL wait_timeout보다 짧게 설정
spring.datasource.hikari.max-lifetime=1800000

# 유휴 연결 타임아웃 (10분)
spring.datasource.hikari.idle-timeout=600000

# 연결 대기 타임아웃 (30초)
spring.datasource.hikari.connection-timeout=30000

# 연결 검증 타임아웃 (5초)
spring.datasource.hikari.validation-timeout=5000

# keepalive 시간 (5분) - 연결 유지를 위한 주기적인 검증
spring.datasource.hikari.keepalive-time=300000

# leak detection threshold (연결 누수 감지 - 1분)
spring.datasource.hikari.leak-detection-threshold=60000

# ========================================
# JPA / Hibernate ??
# ========================================

# SQL ?? ?? ? ???
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Dialect ??
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ========================================
# JWT ??
# ========================================

# JWT ??? ? (???? ?? .env?? ?? ??)
jwt.secret=${JWT_SECRET:mySecretKeyForJwtTokenGenerationAndValidation12345678901234567890}

# JWT ?? ?? (??: 7? = 604800000ms)
jwt.expiration=${JWT_EXPIRATION:604800000}

# ========================================
# ?? ??? ??
# ========================================

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========================================
# ??? ?? ??
# ========================================

spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100

# ========================================
# ???? ??
# ========================================

# ???? ??? ? ??
spring.task.scheduling.pool.size=5

# ========================================
# ??? ??
# ========================================

spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# ========================================
# ?? ??
# ========================================

logging.level.root=WARN
logging.level.com.sportshub=INFO
logging.level.com.example.backend=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.openqa.selenium=WARN

# Naver News API Configuration
# ========================================
naver.api.client-id=T5fb_yaxu9rjmooq3bG8
naver.api.client-secret=orIevt3o4d

frontend.url=${FRONTEND_URL:http://localhost:5173}