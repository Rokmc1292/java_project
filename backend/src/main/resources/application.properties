# ========================================
# ?? ?? ?? ? URL
# ========================================

# ?? ??
server.port=${SERVER_PORT:8080}

# ???? ??
server.servlet.context-path=/

# ?????? ?? URL
app.base-url=${APP_BASE_URL:http://localhost:8080}

# ?? ?? ??
server.servlet.session.timeout=86400s
server.servlet.session.cookie.name=SESSIONID
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# ========================================
# ?????? ??
# ========================================

# MySQL ?????? URL (utf8mb4 -> UTF-8? ??)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/sports_community?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true}

# ?????? ????
spring.datasource.username=${DB_USERNAME:root}

# ?????? ????
spring.datasource.password=${DB_PASSWORD:tjdwns21}

# MySQL ????
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================================
# JPA / Hibernate ??
# ========================================

# DDL ?? ?? ?? (validate / update / none)
spring.jpa.hibernate.ddl-auto=validate

# SQL ?? ?? ? ???
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Dialect ??
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ========================================
# JWT ??
# ========================================

# JWT ??? ? (???? ?? .env?? ?? ??)
jwt.secret=${JWT_SECRET:mySecretKeyForJwtTokenGenerationAndValidation12345678901234567890}

# JWT ?? ?? (??: 7? = 604800000ms)
jwt.expiration=${JWT_EXPIRATION:604800000}

# ========================================
# ?? ??? ??
# ========================================

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========================================
# ??? ?? ??
# ========================================

spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100

# ========================================
# ???? ??
# ========================================

# ???? ??? ? ??
spring.task.scheduling.pool.size=5

# ========================================
# ??? ??
# ========================================

spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# ========================================
# ?? ??
# ========================================

logging.level.root=INFO
logging.level.com.sportshub=INFO
logging.level.com.example.backend=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.openqa.selenium=WARN